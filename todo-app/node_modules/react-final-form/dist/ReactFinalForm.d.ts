import * as React from "react";
import type { FormApi, FormSubscription } from "final-form";
import type { FormProps } from "./types";
import { version } from "../package.json";
export { version };
export declare const all: FormSubscription;
declare function ReactFinalForm<FormValues = Record<string, any>>({ debug, decorators, destroyOnUnregister, form: alternateFormApi, initialValues, initialValuesEqual, keepDirtyOnReinitialize, mutators, onSubmit, subscription, validate, validateOnBlur, ...rest }: FormProps<FormValues>): React.FunctionComponentElement<React.ProviderProps<FormApi<any, Partial<any>> | undefined>>;
export default ReactFinalForm;
