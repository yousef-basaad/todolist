import { r as __toESM, t as require_react } from "./react-BQupsjBO.js";
//#region node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
	return _extends = Object.assign ? Object.assign.bind() : function(n) {
		for (var e = 1; e < arguments.length; e++) {
			var t = arguments[e];
			for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]);
		}
		return n;
	}, _extends.apply(null, arguments);
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(r, e) {
	if (null == r) return {};
	var t = {};
	for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
		if (-1 !== e.indexOf(n)) continue;
		t[n] = r[n];
	}
	return t;
}
//#endregion
//#region node_modules/final-form/dist/final-form.es.js
var import_react = /* @__PURE__ */ __toESM(require_react());
var charCodeOfDot = ".".charCodeAt(0);
var reEscapeChar = /\\(\\)?/g;
var rePropName = RegExp("[^.[\\]]+|\\[(?:([^\"'][^[]*)|([\"'])((?:(?!\\2)[^\\\\]|\\\\.)*?)\\2)\\]|(?=(?:\\.|\\[\\])(?:\\.|\\[\\]|$))", "g");
/**
* Converts `string` to a property path array.
*
* @private
* @param {string} string The string to convert.
* @returns {Array} Returns the property path array.
*/
var stringToPath = function stringToPath(string) {
	var result = [];
	if (string.charCodeAt(0) === charCodeOfDot) result.push("");
	string.replace(rePropName, function(match, expression, quote, subString) {
		var key = match;
		if (quote) key = subString.replace(reEscapeChar, "$1");
		else if (expression) key = expression.trim();
		result.push(key);
		return "";
	});
	return result;
};
var keysCache = {};
var keysRegex = /[.[\]]+/;
var toPath = function toPath(key) {
	if (key === null || key === void 0 || !key.length) return [];
	if (typeof key !== "string") throw new Error("toPath() expects a string");
	if (keysCache[key] == null)
 /**
	* The following patch fixes issue 456, introduced since v4.20.3:
	*
	* Before v4.20.3, i.e. in v4.20.2, a `key` like 'choices[]' would map to ['choices']
	* (e.g. an array of choices used where 'choices[]' is name attribute of an input of type checkbox).
	*
	* Since v4.20.3, a `key` like 'choices[]' would map to ['choices', ''] which is wrong and breaks
	* this kind of inputs e.g. in React.
	*
	* v4.20.3 introduced an unwanted breaking change, this patch fixes it, see the issue at the link below.
	*
	* @see https://github.com/final-form/final-form/issues/456
	*/
	if (key.endsWith("[]")) keysCache[key] = key.split(keysRegex).filter(Boolean);
	else keysCache[key] = stringToPath(key);
	return keysCache[key];
};
var getIn = function getIn(state, complexKey) {
	var path = toPath(complexKey);
	var current = state;
	for (var i = 0; i < path.length; i++) {
		var key = path[i];
		if (current === void 0 || current === null || typeof current !== "object" || Array.isArray(current) && isNaN(Number(key))) return;
		current = current[key];
	}
	return current;
};
function _toPropertyKey(t) {
	var i = _toPrimitive(t, "string");
	return "symbol" == typeof i ? i : i + "";
}
function _toPrimitive(t, r) {
	if ("object" != typeof t || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r);
		if ("object" != typeof i) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return String(t);
}
var _setInRecursor = function setInRecursor(current, index, path, value, destroyArrays) {
	if (index >= path.length) return value;
	var key = path[index];
	if (isNaN(Number(key))) {
		var _extends2;
		if (current === void 0 || current === null) {
			var _ref;
			var _result = _setInRecursor(void 0, index + 1, path, value, destroyArrays);
			return _result === void 0 ? void 0 : (_ref = {}, _ref[key] = _result, _ref);
		}
		if (Array.isArray(current)) throw new Error("Cannot set a non-numeric property on an array");
		var _result2 = _setInRecursor(current[key], index + 1, path, value, destroyArrays);
		if (_result2 === void 0) {
			var numKeys = Object.keys(current).length;
			if (current[key] === void 0 && numKeys === 0) return;
			if (current[key] !== void 0 && numKeys <= 1) if (!isNaN(Number(path[index - 1])) && !destroyArrays) return {};
			else return;
			current[key];
			return _objectWithoutPropertiesLoose(current, [key].map(_toPropertyKey));
		}
		return _extends({}, current, (_extends2 = {}, _extends2[key] = _result2, _extends2));
	}
	var numericKey = Number(key);
	if (current === void 0 || current === null) {
		var _result3 = _setInRecursor(void 0, index + 1, path, value, destroyArrays);
		if (_result3 === void 0) return;
		var _array = [];
		_array[numericKey] = _result3;
		return _array;
	}
	if (!Array.isArray(current)) throw new Error("Cannot set a numeric property on an object");
	var existingValue = current[numericKey];
	var result = _setInRecursor(existingValue, index + 1, path, value, destroyArrays);
	var array = [].concat(current);
	if (destroyArrays && result === void 0) {
		array.splice(numericKey, 1);
		if (array.length === 0) return;
	} else array[numericKey] = result;
	return array;
};
var setIn = function setIn(state, key, value, destroyArrays) {
	if (destroyArrays === void 0) destroyArrays = false;
	if (state === void 0 || state === null) throw new Error("Cannot call setIn() with " + String(state) + " state");
	if (key === void 0 || key === null) throw new Error("Cannot call setIn() with " + String(key) + " key");
	return _setInRecursor(state, 0, toPath(key), value, destroyArrays);
};
var FORM_ERROR = "FINAL_FORM/form-error";
var ARRAY_ERROR = "FINAL_FORM/array-error";
/**
* Converts internal field state to published field state
*/
function publishFieldState(formState, field) {
	var errors = formState.errors, initialValues = formState.initialValues, lastSubmittedValues = formState.lastSubmittedValues, submitErrors = formState.submitErrors, submitFailed = formState.submitFailed, submitSucceeded = formState.submitSucceeded, submitting = formState.submitting, values = formState.values;
	var active = field.active, blur = field.blur, change = field.change, data = field.data, focus = field.focus, modified = field.modified, modifiedSinceLastSubmit = field.modifiedSinceLastSubmit, name = field.name, touched = field.touched, validating = field.validating, visited = field.visited;
	var value = getIn(values, name);
	var error = errors ? getIn(errors, name) : void 0;
	if (error && error["FINAL_FORM/array-error"]) error = error[ARRAY_ERROR];
	var submitError = submitErrors && getIn(submitErrors, name);
	var initial = initialValues && getIn(initialValues, name);
	var pristine = field.isEqual(initial, value);
	var dirtySinceLastSubmit = !!(lastSubmittedValues && !field.isEqual(getIn(lastSubmittedValues, name), value));
	var valid = !error && !submitError;
	return {
		active,
		blur,
		change,
		data,
		dirty: !pristine,
		dirtySinceLastSubmit,
		error,
		focus,
		initial,
		invalid: !valid,
		length: Array.isArray(value) ? value.length : void 0,
		modified,
		modifiedSinceLastSubmit,
		name,
		pristine,
		submitError,
		submitFailed,
		submitSucceeded,
		submitting,
		touched,
		valid,
		value,
		visited,
		validating
	};
}
var fieldSubscriptionItems = [
	"active",
	"data",
	"dirty",
	"dirtySinceLastSubmit",
	"error",
	"initial",
	"invalid",
	"length",
	"modified",
	"modifiedSinceLastSubmit",
	"pristine",
	"submitError",
	"submitFailed",
	"submitSucceeded",
	"submitting",
	"touched",
	"valid",
	"value",
	"visited",
	"validating"
];
var shallowEqual$1 = function shallowEqual(a, b) {
	if (a === b) return true;
	if (typeof a !== "object" || !a || typeof b !== "object" || !b) return false;
	var keysA = Object.keys(a);
	var keysB = Object.keys(b);
	if (keysA.length !== keysB.length) return false;
	var bHasOwnProperty = Object.prototype.hasOwnProperty.bind(b);
	for (var idx = 0; idx < keysA.length; idx++) {
		var key = keysA[idx];
		if (!bHasOwnProperty(key) || a[key] !== b[key]) return false;
	}
	return true;
};
function subscriptionFilter(dest, src, previous, subscription, keys, shallowEqualKeys) {
	var different = false;
	keys.forEach(function(key) {
		if (subscription[key]) {
			dest[key] = src[key];
			if (!previous || (~shallowEqualKeys.indexOf(key) ? !shallowEqual$1(src[key], previous[key]) : src[key] !== previous[key])) different = true;
		}
	});
	return different;
}
var shallowEqualKeys$1 = ["data"];
/**
* Filters items in a FieldState based on a FieldSubscription
*/
var filterFieldState = function filterFieldState(state, previousState, subscription, force) {
	var result = {
		blur: state.blur,
		change: state.change,
		focus: state.focus,
		name: state.name
	};
	return subscriptionFilter(result, state, previousState, subscription, fieldSubscriptionItems, shallowEqualKeys$1) || !previousState || force ? result : void 0;
};
var formSubscriptionItems = [
	"active",
	"dirty",
	"dirtyFields",
	"dirtyFieldsSinceLastSubmit",
	"dirtySinceLastSubmit",
	"error",
	"errors",
	"hasSubmitErrors",
	"hasValidationErrors",
	"initialValues",
	"invalid",
	"modified",
	"modifiedSinceLastSubmit",
	"pristine",
	"submitting",
	"submitError",
	"submitErrors",
	"submitFailed",
	"submitSucceeded",
	"touched",
	"valid",
	"validating",
	"values",
	"visited"
];
var shallowEqualKeys = ["touched", "visited"];
/**
* Filters items in a FormState based on a FormSubscription
*/
function filterFormState(state, previousState, subscription, force) {
	var result = {};
	return subscriptionFilter(result, state, previousState, subscription, formSubscriptionItems, shallowEqualKeys) || !previousState || force ? result : void 0;
}
var memoize = function memoize(fn) {
	var lastArgs;
	var lastResult;
	return function() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		if (!lastArgs || args.length !== lastArgs.length || args.some(function(arg, index) {
			return !shallowEqual$1(lastArgs[index], arg);
		})) {
			lastArgs = args;
			lastResult = fn.apply(void 0, args);
		}
		return lastResult;
	};
};
var isPromise = (function(obj) {
	return !!obj && (typeof obj === "object" || typeof obj === "function") && typeof obj.then === "function";
});
var version$1 = "5.0.0-1";
var tripleEquals = function tripleEquals(a, b) {
	return a === b;
};
var _hasAnyError = function hasAnyError(errors) {
	return Object.keys(errors).some(function(key) {
		var value = errors[key];
		if (value && typeof value === "object" && !(value instanceof Error)) return _hasAnyError(value);
		return typeof value !== "undefined";
	});
};
function convertToExternalFormState(_ref) {
	var active = _ref.active, dirtySinceLastSubmit = _ref.dirtySinceLastSubmit, modifiedSinceLastSubmit = _ref.modifiedSinceLastSubmit, error = _ref.error, errors = _ref.errors, initialValues = _ref.initialValues, pristine = _ref.pristine, submitting = _ref.submitting, submitFailed = _ref.submitFailed, submitSucceeded = _ref.submitSucceeded, submitError = _ref.submitError, submitErrors = _ref.submitErrors, valid = _ref.valid, validating = _ref.validating, values = _ref.values;
	return {
		active,
		dirty: !pristine,
		dirtySinceLastSubmit,
		modifiedSinceLastSubmit,
		error,
		errors,
		hasSubmitErrors: !!(submitError || submitErrors && _hasAnyError(submitErrors)),
		hasValidationErrors: !!(error || errors && _hasAnyError(errors)),
		invalid: !valid,
		initialValues,
		pristine,
		submitting,
		submitFailed,
		submitSucceeded,
		submitError,
		submitErrors,
		valid,
		validating: validating > 0,
		values
	};
}
function notifySubscriber(subscriber, subscription, state, lastState, filter, force) {
	var notification = filter(state, lastState, subscription, force);
	if (notification) {
		subscriber(notification);
		return true;
	}
	return false;
}
function notify(_ref2, state, lastState, filter, force) {
	var entries = _ref2.entries;
	Object.keys(entries).forEach(function(key) {
		var entry = entries[Number(key)];
		// istanbul ignore next
		if (entry) {
			var subscription = entry.subscription, subscriber = entry.subscriber, notified = entry.notified;
			if (notifySubscriber(subscriber, subscription, state, lastState, filter, force || !notified)) entry.notified = true;
		}
	});
}
function createForm(config) {
	if (!config) throw new Error("No config specified");
	var debug = config.debug, destroyOnUnregister = config.destroyOnUnregister, keepDirtyOnReinitialize = config.keepDirtyOnReinitialize, initialValues = config.initialValues, mutators = config.mutators, onSubmit = config.onSubmit, validate = config.validate, validateOnBlur = config.validateOnBlur, callbackScheduler = config.callbackScheduler;
	if (!onSubmit) throw new Error("No onSubmit function specified");
	var state = {
		subscribers: {
			index: 0,
			entries: {}
		},
		fieldSubscribers: {},
		fields: {},
		formState: {
			asyncErrors: {},
			dirtySinceLastSubmit: false,
			modifiedSinceLastSubmit: false,
			errors: {},
			initialValues: initialValues && _extends({}, initialValues),
			pristine: true,
			submitting: false,
			submitFailed: false,
			submitSucceeded: false,
			resetWhileSubmitting: false,
			valid: true,
			validating: 0,
			values: initialValues ? _extends({}, initialValues) : {}
		},
		lastFormState: void 0
	};
	var inBatch = 0;
	var validationPaused = false;
	var validationBlocked = false;
	var preventNotificationWhileValidationPaused = false;
	var nextAsyncValidationKey = 0;
	var asyncValidationPromises = {};
	var pendingAsyncCallbacks = [];
	var asyncCallbacksScheduled = false;
	var clearAsyncValidationPromise = function clearAsyncValidationPromise(key) {
		return function(result) {
			delete asyncValidationPromises[key];
			return result;
		};
	};
	var scheduleAsyncCallbacks = function scheduleAsyncCallbacks() {
		if (!asyncCallbacksScheduled && pendingAsyncCallbacks.length > 0) {
			asyncCallbacksScheduled = true;
			(callbackScheduler || function(callback) {
				return setTimeout(callback, 0);
			})(function() {
				var callbacks = [].concat(pendingAsyncCallbacks);
				pendingAsyncCallbacks.length = 0;
				asyncCallbacksScheduled = false;
				callbacks.forEach(function(callback) {
					return callback();
				});
			});
		}
	};
	var executeCallback = function executeCallback(callback, isAsync) {
		if (isAsync) {
			pendingAsyncCallbacks.push(callback);
			scheduleAsyncCallbacks();
		} else callback();
	};
	var changeValue = function changeValue(state, name, mutate) {
		var after = mutate(getIn(state.formState.values, name));
		state.formState.values = setIn(state.formState.values, name, after) || {};
	};
	var renameField = function renameField(state, from, to) {
		if (state.fields[from]) {
			var _extends2, _extends3;
			state.fields = _extends({}, state.fields, (_extends2 = {}, _extends2[to] = _extends({}, state.fields[from], {
				name: to,
				blur: function blur() {
					return api.blur(to);
				},
				change: function change(value) {
					return api.change(to, value);
				},
				focus: function focus() {
					return api.focus(to);
				},
				lastFieldState: void 0
			}), _extends2));
			delete state.fields[from];
			state.fieldSubscribers = _extends({}, state.fieldSubscribers, (_extends3 = {}, _extends3[to] = state.fieldSubscribers[from], _extends3));
			delete state.fieldSubscribers[from];
			var value = getIn(state.formState.values, from);
			state.formState.values = setIn(state.formState.values, from, void 0) || {};
			state.formState.values = setIn(state.formState.values, to, value) || {};
			delete state.lastFormState;
		}
	};
	var getMutatorApi = function getMutatorApi(key) {
		return function() {
			// istanbul ignore next
			if (mutators) {
				var mutatableState = {
					formState: state.formState,
					fields: state.fields,
					fieldSubscribers: state.fieldSubscribers,
					lastFormState: state.lastFormState
				};
				for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
				var returnValue = mutators[key](args, mutatableState, {
					changeValue,
					getIn,
					renameField,
					resetFieldState: api.resetFieldState,
					setIn,
					shallowEqual: shallowEqual$1
				});
				state.formState = mutatableState.formState;
				state.fields = mutatableState.fields;
				state.fieldSubscribers = mutatableState.fieldSubscribers;
				state.lastFormState = mutatableState.lastFormState;
				runValidation(void 0, function() {
					notifyFieldListeners(void 0);
					_notifyFormListeners();
				});
				return returnValue;
			}
		};
	};
	var mutatorsApi = mutators ? Object.keys(mutators).reduce(function(result, key) {
		result[key] = getMutatorApi(key);
		return result;
	}, {}) : {};
	var runRecordLevelValidation = function runRecordLevelValidation(setErrors) {
		var promises = [];
		if (validate) {
			var errorsOrPromise = validate(_extends({}, state.formState.values));
			if (isPromise(errorsOrPromise)) promises.push(errorsOrPromise.then(function(errors) {
				return setErrors(errors, true);
			}));
			else setErrors(errorsOrPromise, false);
		}
		return promises;
	};
	var getValidators = function getValidators(field) {
		return Object.keys(field.validators).reduce(function(result, index) {
			var validator = field.validators[Number(index)]();
			if (validator) result.push(validator);
			return result;
		}, []);
	};
	var runFieldLevelValidation = function runFieldLevelValidation(field, setError) {
		var promises = [];
		var validators = getValidators(field);
		if (validators.length) {
			var error;
			validators.forEach(function(validator) {
				var errorOrPromise = validator(getIn(state.formState.values, field.name), state.formState.values, validator.length === 0 || validator.length === 3 ? publishFieldState(state.formState, state.fields[field.name]) : void 0);
				if (errorOrPromise && isPromise(errorOrPromise)) {
					field.validating = true;
					var promise = errorOrPromise.then(function(error) {
						if (state.fields[field.name]) {
							state.fields[field.name].validating = false;
							setError(error);
						}
					});
					promises.push(promise);
				} else if (!error) error = errorOrPromise;
			});
			setError(error);
		}
		return promises;
	};
	var runValidation = function runValidation(fieldChanged, callback) {
		if (validationPaused) {
			validationBlocked = true;
			callback();
			return;
		}
		var fields = state.fields, formState = state.formState;
		var safeFields = _extends({}, fields);
		var fieldKeys = Object.keys(safeFields);
		if (!validate && !fieldKeys.some(function(key) {
			return getValidators(safeFields[key]).length;
		})) {
			callback();
			return;
		}
		var limitedFieldLevelValidation = false;
		if (fieldChanged) {
			var changedField = safeFields[fieldChanged];
			if (changedField) {
				var validateFields = changedField.validateFields;
				if (validateFields) {
					limitedFieldLevelValidation = true;
					fieldKeys = validateFields.length ? validateFields.concat(fieldChanged) : [fieldChanged];
				}
			}
		}
		var recordLevelErrors = {};
		var asyncRecordLevelErrors = {};
		var fieldLevelErrors = {};
		var promises = [].concat(runRecordLevelValidation(function(errors, wasAsync) {
			if (wasAsync) asyncRecordLevelErrors = errors || {};
			else recordLevelErrors = errors || {};
		}), fieldKeys.reduce(function(result, name) {
			return result.concat(runFieldLevelValidation(fields[name], function(error) {
				fieldLevelErrors[name] = error;
			}));
		}, []));
		var hasAsyncValidations = promises.length > 0;
		var asyncValidationPromiseKey = ++nextAsyncValidationKey;
		var promise = Promise.all(promises).then(clearAsyncValidationPromise(asyncValidationPromiseKey));
		if (hasAsyncValidations) asyncValidationPromises[asyncValidationPromiseKey] = promise;
		var processErrors = function processErrors(afterAsync) {
			var merged = _extends({}, limitedFieldLevelValidation ? formState.errors : {}, recordLevelErrors, afterAsync ? asyncRecordLevelErrors : formState.asyncErrors);
			var forEachError = function forEachError(fn) {
				fieldKeys.forEach(function(name) {
					if (fields[name]) {
						var recordLevelError = getIn(recordLevelErrors, name);
						var errorFromParent = getIn(merged, name);
						var hasFieldLevelValidation = getValidators(safeFields[name]).length;
						var fieldLevelError = fieldLevelErrors[name];
						fn(name, hasFieldLevelValidation && fieldLevelError || validate && recordLevelError || (!recordLevelError && !limitedFieldLevelValidation ? errorFromParent : void 0));
					}
				});
			};
			forEachError(function(name, error) {
				merged = setIn(merged, name, error) || {};
			});
			forEachError(function(name, error) {
				if (error && error["FINAL_FORM/array-error"]) {
					var existing = getIn(merged, name);
					var copy = [].concat(existing);
					copy[ARRAY_ERROR] = error[ARRAY_ERROR];
					merged = setIn(merged, name, copy);
				}
			});
			if (!shallowEqual$1(formState.errors, merged)) formState.errors = merged;
			if (afterAsync) formState.asyncErrors = asyncRecordLevelErrors;
			formState.error = recordLevelErrors[FORM_ERROR];
		};
		if (hasAsyncValidations) {
			state.formState.validating++;
			callback();
		}
		processErrors(false);
		callback();
		if (hasAsyncValidations) {
			var afterPromise = function afterPromise() {
				state.formState.validating--;
				callback();
				if (state.formState.validating === 0 && state.lastFormState.validating) _notifyFormListeners();
			};
			promise.then(function() {
				if (nextAsyncValidationKey > asyncValidationPromiseKey) return;
				processErrors(true);
			}).then(afterPromise, afterPromise);
		}
	};
	var notifyFieldListeners = function notifyFieldListeners(name) {
		if (inBatch) return;
		var fields = state.fields, fieldSubscribers = state.fieldSubscribers, formState = state.formState;
		var safeFields = _extends({}, fields);
		var notifyField = function notifyField(name) {
			var field = safeFields[name];
			var fieldState = publishFieldState(formState, field);
			var lastFieldState = field.lastFieldState;
			field.lastFieldState = fieldState;
			var fieldSubscriber = fieldSubscribers[name];
			if (fieldSubscriber) notify(fieldSubscriber, fieldState, lastFieldState, filterFieldState, lastFieldState === void 0);
		};
		if (name) notifyField(name);
		else Object.keys(safeFields).forEach(notifyField);
	};
	var markAllFieldsTouched = function markAllFieldsTouched() {
		Object.keys(state.fields).forEach(function(key) {
			state.fields[key].touched = true;
		});
	};
	var hasSyncErrors = function hasSyncErrors() {
		return !!(state.formState.error || _hasAnyError(state.formState.errors));
	};
	var calculateNextFormState = function calculateNextFormState() {
		var fields = state.fields, formState = state.formState, lastFormState = state.lastFormState;
		var safeFields = _extends({}, fields);
		var safeFieldKeys = Object.keys(safeFields);
		var foundDirty = false;
		var dirtyFields = safeFieldKeys.reduce(function(result, key) {
			if (!safeFields[key].isEqual(getIn(formState.values, key), getIn(formState.initialValues || {}, key))) {
				foundDirty = true;
				result[key] = true;
			}
			return result;
		}, {});
		var dirtyFieldsSinceLastSubmit = safeFieldKeys.reduce(function(result, key) {
			// istanbul ignore next
			var nonNullLastSubmittedValues = formState.lastSubmittedValues || {};
			if (!safeFields[key].isEqual(getIn(formState.values, key), getIn(nonNullLastSubmittedValues, key))) result[key] = true;
			return result;
		}, {});
		formState.pristine = !foundDirty;
		formState.dirtySinceLastSubmit = !!(formState.lastSubmittedValues && Object.values(dirtyFieldsSinceLastSubmit).some(function(value) {
			return value;
		}));
		formState.modifiedSinceLastSubmit = !!(formState.lastSubmittedValues && Object.keys(safeFields).some(function(value) {
			return safeFields[value].modifiedSinceLastSubmit;
		}));
		formState.valid = !formState.error && !formState.submitError && !_hasAnyError(formState.errors) && !(formState.submitErrors && _hasAnyError(formState.submitErrors));
		var nextFormState = convertToExternalFormState(formState);
		var _safeFieldKeys$reduce = safeFieldKeys.reduce(function(result, key) {
			result.modified[key] = safeFields[key].modified;
			result.touched[key] = safeFields[key].touched;
			result.visited[key] = safeFields[key].visited;
			return result;
		}, {
			modified: {},
			touched: {},
			visited: {}
		}), modified = _safeFieldKeys$reduce.modified, touched = _safeFieldKeys$reduce.touched, visited = _safeFieldKeys$reduce.visited;
		nextFormState.dirtyFields = lastFormState && shallowEqual$1(lastFormState.dirtyFields, dirtyFields) ? lastFormState.dirtyFields : dirtyFields;
		nextFormState.dirtyFieldsSinceLastSubmit = lastFormState && shallowEqual$1(lastFormState.dirtyFieldsSinceLastSubmit, dirtyFieldsSinceLastSubmit) ? lastFormState.dirtyFieldsSinceLastSubmit : dirtyFieldsSinceLastSubmit;
		nextFormState.modified = lastFormState && shallowEqual$1(lastFormState.modified, modified) ? lastFormState.modified : modified;
		nextFormState.touched = lastFormState && shallowEqual$1(lastFormState.touched, touched) ? lastFormState.touched : touched;
		nextFormState.visited = lastFormState && shallowEqual$1(lastFormState.visited, visited) ? lastFormState.visited : visited;
		return lastFormState && shallowEqual$1(lastFormState, nextFormState) ? lastFormState : nextFormState;
	};
	var callDebug = function callDebug() {
		return debug && debug(calculateNextFormState(), Object.keys(state.fields).reduce(function(result, key) {
			result[key] = state.fields[key];
			return result;
		}, {}));
	};
	var notifying = false;
	var scheduleNotification = false;
	var _notifyFormListeners = function notifyFormListeners() {
		if (notifying) scheduleNotification = true;
		else {
			notifying = true;
			callDebug();
			if (!inBatch && !(validationPaused && preventNotificationWhileValidationPaused)) {
				var lastFormState = state.lastFormState;
				var nextFormState = calculateNextFormState();
				if (nextFormState !== lastFormState) {
					state.lastFormState = nextFormState;
					notify(state.subscribers, nextFormState, lastFormState, filterFormState);
				}
			}
			notifying = false;
			if (scheduleNotification) {
				scheduleNotification = false;
				_notifyFormListeners();
			}
		}
	};
	var beforeSubmit = function beforeSubmit() {
		return Object.keys(state.fields).some(function(name) {
			return state.fields[name].beforeSubmit && state.fields[name].beforeSubmit() === false;
		});
	};
	var afterSubmit = function afterSubmit() {
		return Object.keys(state.fields).forEach(function(name) {
			return state.fields[name].afterSubmit && state.fields[name].afterSubmit();
		});
	};
	var resetModifiedAfterSubmit = function resetModifiedAfterSubmit() {
		return Object.keys(state.fields).forEach(function(key) {
			return state.fields[key].modifiedSinceLastSubmit = false;
		});
	};
	runValidation(void 0, function() {
		_notifyFormListeners();
	});
	var api = {
		batch: function batch(fn) {
			inBatch++;
			fn();
			inBatch--;
			notifyFieldListeners(void 0);
			_notifyFormListeners();
		},
		blur: function blur(name) {
			var fields = state.fields, formState = state.formState;
			var previous = fields[name];
			if (previous) {
				delete formState.active;
				fields[name] = _extends({}, previous, {
					active: false,
					touched: true
				});
				if (validateOnBlur) runValidation(name, function() {
					notifyFieldListeners(void 0);
					_notifyFormListeners();
				});
				else {
					notifyFieldListeners(void 0);
					_notifyFormListeners();
				}
			}
		},
		change: function change(name, value) {
			var fields = state.fields, formState = state.formState;
			if (getIn(formState.values, name) !== value) {
				changeValue(state, name, function() {
					return value;
				});
				var previous = fields[name];
				if (previous) fields[name] = _extends({}, previous, {
					modified: true,
					modifiedSinceLastSubmit: !!formState.lastSubmittedValues
				});
				if (validateOnBlur) {
					notifyFieldListeners(void 0);
					_notifyFormListeners();
				} else runValidation(name, function() {
					notifyFieldListeners(void 0);
					_notifyFormListeners();
				});
			}
		},
		get destroyOnUnregister() {
			return !!destroyOnUnregister;
		},
		set destroyOnUnregister(value) {
			destroyOnUnregister = value;
		},
		focus: function focus(name) {
			var field = state.fields[name];
			if (field && !field.active) {
				state.formState.active = name;
				field.active = true;
				field.visited = true;
				notifyFieldListeners(void 0);
				_notifyFormListeners();
			}
		},
		mutators: mutatorsApi,
		getFieldState: function getFieldState(name) {
			var field = state.fields[name];
			return field && field.lastFieldState;
		},
		getRegisteredFields: function getRegisteredFields() {
			return Object.keys(state.fields);
		},
		getState: function getState() {
			return calculateNextFormState();
		},
		initialize: function initialize(data) {
			var fields = state.fields, formState = state.formState;
			var safeFields = _extends({}, fields);
			var values = typeof data === "function" ? data(formState.values) : data;
			if (!keepDirtyOnReinitialize) formState.values = values;
			var savedDirtyValues = keepDirtyOnReinitialize ? Object.keys(safeFields).reduce(function(result, key) {
				if (!safeFields[key].isEqual(getIn(formState.values, key), getIn(formState.initialValues || {}, key))) result[key] = getIn(formState.values, key);
				return result;
			}, {}) : {};
			formState.initialValues = values;
			formState.values = values;
			Object.keys(savedDirtyValues).forEach(function(key) {
				formState.values = setIn(formState.values, key, savedDirtyValues[key]) || {};
			});
			runValidation(void 0, function() {
				notifyFieldListeners(void 0);
				_notifyFormListeners();
			});
		},
		isValidationPaused: function isValidationPaused() {
			return validationPaused;
		},
		pauseValidation: function pauseValidation(preventNotification) {
			if (preventNotification === void 0) preventNotification = true;
			validationPaused = true;
			preventNotificationWhileValidationPaused = preventNotification;
		},
		registerField: function registerField(name, subscriber, subscription, fieldConfig) {
			if (subscription === void 0) subscription = {};
			if (!state.fieldSubscribers[name]) state.fieldSubscribers[name] = {
				index: 0,
				entries: {}
			};
			var index = state.fieldSubscribers[name].index++;
			state.fieldSubscribers[name].entries[index] = {
				subscriber: memoize(subscriber),
				subscription,
				notified: false
			};
			var field = state.fields[name] || {
				active: false,
				afterSubmit: fieldConfig && fieldConfig.afterSubmit,
				beforeSubmit: fieldConfig && fieldConfig.beforeSubmit,
				data: fieldConfig && fieldConfig.data || {},
				isEqual: fieldConfig && fieldConfig.isEqual || tripleEquals,
				lastFieldState: void 0,
				modified: false,
				modifiedSinceLastSubmit: false,
				name,
				touched: false,
				valid: true,
				validateFields: fieldConfig && fieldConfig.validateFields,
				validators: {},
				validating: false,
				visited: false,
				blur: function blur() {
					return api.blur(name);
				},
				change: function change(value) {
					return api.change(name, value);
				},
				focus: function focus() {
					return api.focus(name);
				}
			};
			field.blur = field.blur || function() {
				return api.blur(name);
			};
			field.change = field.change || function(value) {
				return api.change(name, value);
			};
			field.focus = field.focus || function() {
				return api.focus(name);
			};
			state.fields[name] = field;
			var haveValidator = false;
			var silent = fieldConfig && fieldConfig.silent;
			var isAsync = fieldConfig && fieldConfig.async;
			var notify = function notify() {
				executeCallback(function callback() {
					if (silent && state.fields[name]) notifyFieldListeners(name);
					else {
						_notifyFormListeners();
						notifyFieldListeners(void 0);
					}
				}, !!isAsync);
			};
			if (fieldConfig) {
				haveValidator = !!(fieldConfig.getValidator && fieldConfig.getValidator());
				if (fieldConfig.getValidator) state.fields[name].validators[index] = fieldConfig.getValidator;
				var noValueInFormState = getIn(state.formState.values, name) === void 0;
				if (fieldConfig.initialValue !== void 0 && (noValueInFormState || getIn(state.formState.values, name) === getIn(state.formState.initialValues, name))) {
					state.formState.initialValues = setIn(state.formState.initialValues || {}, name, fieldConfig.initialValue);
					state.formState.values = setIn(state.formState.values, name, fieldConfig.initialValue);
					runValidation(void 0, notify);
				}
				if (fieldConfig.defaultValue !== void 0 && fieldConfig.initialValue === void 0 && getIn(state.formState.initialValues, name) === void 0 && noValueInFormState) state.formState.values = setIn(state.formState.values, name, fieldConfig.defaultValue);
			}
			if (haveValidator) runValidation(void 0, notify);
			else notify();
			return function() {
				var validatorRemoved = false;
				// istanbul ignore next
				if (state.fields[name]) {
					validatorRemoved = !!(state.fields[name].validators[index] && state.fields[name].validators[index]());
					delete state.fields[name].validators[index];
				}
				var hasFieldSubscribers = !!state.fieldSubscribers[name];
				if (hasFieldSubscribers) delete state.fieldSubscribers[name].entries[index];
				var lastOne = hasFieldSubscribers && !Object.keys(state.fieldSubscribers[name].entries).length;
				if (lastOne) {
					delete state.fieldSubscribers[name];
					delete state.fields[name];
					if (validatorRemoved) state.formState.errors = setIn(state.formState.errors, name, void 0) || {};
					if (destroyOnUnregister) state.formState.values = setIn(state.formState.values, name, void 0, true) || {};
				}
				if (!silent) {
					if (validatorRemoved) runValidation(void 0, function() {
						_notifyFormListeners();
						notifyFieldListeners(void 0);
					});
					else if (lastOne) _notifyFormListeners();
				}
			};
		},
		reset: function reset(initialValues) {
			if (initialValues === void 0) initialValues = state.formState.initialValues;
			if (state.formState.submitting) state.formState.resetWhileSubmitting = true;
			state.formState.submitFailed = false;
			state.formState.submitSucceeded = false;
			delete state.formState.submitError;
			delete state.formState.submitErrors;
			delete state.formState.lastSubmittedValues;
			api.initialize(initialValues || {});
		},
		resetFieldState: function resetFieldState(name) {
			state.fields[name] = _extends({}, state.fields[name], {
				active: false,
				lastFieldState: void 0,
				modified: false,
				touched: false,
				valid: true,
				validating: false,
				visited: false
			});
			runValidation(void 0, function() {
				notifyFieldListeners(void 0);
				_notifyFormListeners();
			});
		},
		restart: function restart(initialValues) {
			if (initialValues === void 0) initialValues = state.formState.initialValues;
			api.batch(function() {
				for (var name in state.fields) {
					api.resetFieldState(name);
					state.fields[name] = _extends({}, state.fields[name], {
						active: false,
						lastFieldState: void 0,
						modified: false,
						modifiedSinceLastSubmit: false,
						touched: false,
						valid: true,
						validating: false,
						visited: false
					});
				}
				api.reset(initialValues || {});
			});
		},
		resumeValidation: function resumeValidation() {
			validationPaused = false;
			preventNotificationWhileValidationPaused = false;
			if (validationBlocked) runValidation(void 0, function() {
				notifyFieldListeners(void 0);
				_notifyFormListeners();
			});
			validationBlocked = false;
		},
		setConfig: function setConfig(name, value) {
			switch (name) {
				case "debug":
					debug = typeof value === "function" ? value : void 0;
					break;
				case "destroyOnUnregister":
					destroyOnUnregister = value;
					break;
				case "initialValues":
					api.initialize(value);
					break;
				case "keepDirtyOnReinitialize":
					keepDirtyOnReinitialize = value;
					break;
				case "mutators":
					mutators = value;
					if (value) {
						Object.keys(mutatorsApi).forEach(function(key) {
							if (!(key in value)) delete mutatorsApi[key];
						});
						Object.keys(value).forEach(function(key) {
							mutatorsApi[key] = getMutatorApi(key);
						});
					} else Object.keys(mutatorsApi).forEach(function(key) {
						delete mutatorsApi[key];
					});
					break;
				case "onSubmit":
					onSubmit = value;
					break;
				case "validate":
					validate = value;
					runValidation(void 0, function() {
						notifyFieldListeners(void 0);
						_notifyFormListeners();
					});
					break;
				case "validateOnBlur":
					validateOnBlur = value;
					break;
				case "callbackScheduler":
					callbackScheduler = value;
					break;
				default: throw new Error("Unrecognised option " + name);
			}
		},
		setCallbackScheduler: function setCallbackScheduler(scheduler) {
			callbackScheduler = scheduler;
		},
		submit: function submit() {
			var formState = state.formState;
			if (formState.submitting) return Promise.resolve(void 0);
			delete formState.submitErrors;
			delete formState.submitError;
			formState.lastSubmittedValues = _extends({}, formState.values);
			if (hasSyncErrors()) {
				markAllFieldsTouched();
				resetModifiedAfterSubmit();
				state.formState.submitFailed = true;
				_notifyFormListeners();
				notifyFieldListeners(void 0);
				return Promise.resolve(void 0);
			}
			var asyncValidationPromisesKeys = Object.keys(asyncValidationPromises);
			if (asyncValidationPromisesKeys.length) return Promise.all(asyncValidationPromisesKeys.map(function(key) {
				return asyncValidationPromises[Number(key)];
			})).then(function() {
				return api.submit();
			}, function(err) {
				console.error(err);
			});
			if (beforeSubmit()) return Promise.resolve(void 0);
			var resolvePromise;
			var completeCalled = false;
			var complete = function complete(errors) {
				formState.submitting = false;
				var resetWhileSubmitting = formState.resetWhileSubmitting;
				if (resetWhileSubmitting) formState.resetWhileSubmitting = false;
				if (errors && _hasAnyError(errors)) {
					formState.submitFailed = true;
					formState.submitSucceeded = false;
					formState.submitErrors = errors;
					formState.submitError = errors[FORM_ERROR];
					markAllFieldsTouched();
				} else {
					if (!resetWhileSubmitting) {
						formState.submitFailed = false;
						formState.submitSucceeded = true;
					}
					afterSubmit();
				}
				_notifyFormListeners();
				notifyFieldListeners(void 0);
				completeCalled = true;
				if (resolvePromise) resolvePromise(errors);
				return errors;
			};
			formState.submitting = true;
			formState.submitFailed = false;
			formState.submitSucceeded = false;
			formState.lastSubmittedValues = _extends({}, formState.values);
			resetModifiedAfterSubmit();
			var result = onSubmit(formState.values, api, complete);
			if (!completeCalled) if (result && isPromise(result)) {
				_notifyFormListeners();
				notifyFieldListeners(void 0);
				return result.then(function(value) {
					complete(value);
					return value;
				}, function(error) {
					complete(void 0);
					throw error;
				});
			} else if (onSubmit.length >= 3) {
				_notifyFormListeners();
				notifyFieldListeners(void 0);
				return new Promise(function(resolve) {
					resolvePromise = resolve;
				});
			} else complete(result);
			return Promise.resolve(void 0);
		},
		subscribe: function subscribe(subscriber, subscription) {
			if (!subscriber) throw new Error("No callback given.");
			if (!subscription) throw new Error("No subscription provided. What values do you want to listen to?");
			var memoized = memoize(subscriber);
			var subscribers = state.subscribers;
			var index = subscribers.index++;
			subscribers.entries[index] = {
				subscriber: memoized,
				subscription,
				notified: false
			};
			var nextFormState = calculateNextFormState();
			notifySubscriber(memoized, subscription, nextFormState, nextFormState, filterFormState, true);
			return function() {
				delete subscribers.entries[index];
			};
		},
		subscribeFieldState: function subscribeFieldState(name, onChange, subscription) {
			return api.registerField(name, onChange, subscription);
		},
		getFieldSnapshot: function getFieldSnapshot(name) {
			var field = state.fields[name];
			if (!field) return;
			return publishFieldState(state.formState, field);
		},
		subscribeFormState: function subscribeFormState(onChange, subscription) {
			return api.subscribe(onChange, subscription);
		},
		getFormSnapshot: function getFormSnapshot() {
			return calculateNextFormState();
		}
	};
	return api;
}
//#endregion
//#region node_modules/react-final-form/dist/react-final-form.es.js
var _excluded$3 = [
	"render",
	"children",
	"component"
];
function renderComponent(props, lazyProps, name) {
	var render = props.render, children = props.children, component = props.component, rest = _objectWithoutPropertiesLoose(props, _excluded$3);
	if (component) return /* @__PURE__ */ import_react.createElement(component, Object.assign(lazyProps, rest, {
		children,
		render
	}));
	if (render) {
		var _result = {};
		Object.defineProperties(_result, Object.getOwnPropertyDescriptors(lazyProps));
		var _restDescriptors = Object.getOwnPropertyDescriptors(rest);
		for (var key in _restDescriptors) if (!(key in _result)) Object.defineProperty(_result, key, _restDescriptors[key]);
		if (children !== void 0) _result.children = children;
		return render(_result);
	}
	if (typeof children !== "function") throw new Error("Must specify either a render prop, a render function as children, or a component prop to " + name);
	var result = {};
	Object.defineProperties(result, Object.getOwnPropertyDescriptors(lazyProps));
	var restDescriptors = Object.getOwnPropertyDescriptors(rest);
	for (var _key in restDescriptors) if (!(_key in result)) Object.defineProperty(result, _key, restDescriptors[_key]);
	return children(result);
}
function useWhenValueChanges(value, callback, isEqual) {
	if (isEqual === void 0) isEqual = function isEqual(a, b) {
		return a === b;
	};
	var previous = import_react.useRef(value);
	import_react.useEffect(function() {
		if (!isEqual(value, previous.current)) {
			callback();
			previous.current = value;
		}
	});
}
/**
* A simple hook to create a constant value that lives for
* the lifetime of the component.
*
* Plagiarized from https://github.com/Andarist/use-constant
*
* Do NOT reuse this code unless you know what you're doing.
* Use Andarist's hook; it's more fault tolerant to things like
* falsy values.
*
* @param init - A function to generate the value
*/
function useConstant(init) {
	var ref = import_react.useRef(void 0);
	if (!ref.current) ref.current = init();
	return ref.current;
}
var shallowEqual = function shallowEqual(a, b) {
	if (a === b) return true;
	if (typeof a !== "object" || !a || typeof b !== "object" || !b) return false;
	var keysA = Object.keys(a);
	var keysB = Object.keys(b);
	if (keysA.length !== keysB.length) return false;
	var bHasOwnProperty = Object.prototype.hasOwnProperty.bind(b);
	for (var idx = 0; idx < keysA.length; idx++) {
		var key = keysA[idx];
		if (!bHasOwnProperty(key) || a[key] !== b[key]) return false;
	}
	return true;
};
var isSyntheticEvent = function isSyntheticEvent(candidate) {
	return !!(candidate && typeof candidate.stopPropagation === "function");
};
var ReactFinalFormContext = /* @__PURE__ */ import_react.createContext(void 0);
var addLazyState = function addLazyState(dest, state, keys) {
	keys.forEach(function(key) {
		Object.defineProperty(dest, key, {
			get: function get() {
				return state[key];
			},
			enumerable: true
		});
	});
};
var addLazyFormState = function addLazyFormState(dest, state) {
	return addLazyState(dest, state, [
		"active",
		"dirty",
		"dirtyFields",
		"dirtySinceLastSubmit",
		"dirtyFieldsSinceLastSubmit",
		"error",
		"errors",
		"hasSubmitErrors",
		"hasValidationErrors",
		"initialValues",
		"invalid",
		"modified",
		"modifiedSinceLastSubmit",
		"pristine",
		"submitError",
		"submitErrors",
		"submitFailed",
		"submitSucceeded",
		"submitting",
		"touched",
		"valid",
		"validating",
		"values",
		"visited"
	]);
};
var addLazyFieldMetaState = function addLazyFieldMetaState(dest, state) {
	return addLazyState(dest, state, [
		"active",
		"data",
		"dirty",
		"dirtySinceLastSubmit",
		"error",
		"initial",
		"invalid",
		"length",
		"modified",
		"modifiedSinceLastSubmit",
		"pristine",
		"submitError",
		"submitFailed",
		"submitSucceeded",
		"submitting",
		"touched",
		"valid",
		"validating",
		"visited"
	]);
};
var version = "7.0.0";
var _excluded$2 = [
	"debug",
	"decorators",
	"destroyOnUnregister",
	"form",
	"initialValues",
	"initialValuesEqual",
	"keepDirtyOnReinitialize",
	"mutators",
	"onSubmit",
	"subscription",
	"validate",
	"validateOnBlur"
];
var versions = {
	"final-form": version$1,
	"react-final-form": version
};
var all$1 = formSubscriptionItems.reduce(function(result, key) {
	result[key] = true;
	return result;
}, {});
function ReactFinalForm(_ref) {
	var debug = _ref.debug, _ref$decorators = _ref.decorators, decorators = _ref$decorators === void 0 ? [] : _ref$decorators, destroyOnUnregister = _ref.destroyOnUnregister, alternateFormApi = _ref.form, initialValues = _ref.initialValues, initialValuesEqual = _ref.initialValuesEqual, keepDirtyOnReinitialize = _ref.keepDirtyOnReinitialize, mutators = _ref.mutators, onSubmit = _ref.onSubmit, _ref$subscription = _ref.subscription, subscription = _ref$subscription === void 0 ? all$1 : _ref$subscription, validate = _ref.validate, validateOnBlur = _ref.validateOnBlur, rest = _objectWithoutPropertiesLoose(_ref, _excluded$2);
	var config = {
		debug,
		destroyOnUnregister,
		initialValues,
		keepDirtyOnReinitialize,
		mutators,
		onSubmit,
		validate,
		validateOnBlur
	};
	var form = useConstant(function() {
		var f = alternateFormApi || createForm(config);
		f.pauseValidation();
		return f;
	});
	var _React$useState = import_react.useState(function() {
		return form.getState();
	}), state = _React$useState[0], setState = _React$useState[1];
	var stateRef = import_react.useRef(state);
	stateRef.current = state;
	import_react.useEffect(function() {
		form.isValidationPaused() && form.resumeValidation();
		var unsubscriptions = [form.subscribe(function(s) {
			setState(function(prevState) {
				if (!shallowEqual(s, prevState)) return s;
				return prevState;
			});
		}, subscription)].concat(decorators ? decorators.map(function(decorator) {
			return decorator(form);
		}) : []);
		return function() {
			form.pauseValidation();
			unsubscriptions.reverse().forEach(function(unsubscribe) {
				return unsubscribe();
			});
		};
	}, []);
	useWhenValueChanges(decorators, function() {
		console.error("Form decorators should not change from one render to the next as new values will be ignored");
	}, shallowEqual);
	useWhenValueChanges(debug, function() {
		form.setConfig("debug", debug);
	});
	useWhenValueChanges(destroyOnUnregister, function() {
		form.destroyOnUnregister = !!destroyOnUnregister;
	});
	useWhenValueChanges(keepDirtyOnReinitialize, function() {
		form.setConfig("keepDirtyOnReinitialize", keepDirtyOnReinitialize);
	});
	useWhenValueChanges(initialValues, function() {
		form.setConfig("initialValues", initialValues);
	}, initialValuesEqual || shallowEqual);
	useWhenValueChanges(mutators, function() {
		form.setConfig("mutators", mutators);
	});
	useWhenValueChanges(onSubmit, function() {
		form.setConfig("onSubmit", onSubmit);
	});
	useWhenValueChanges(validate, function() {
		form.setConfig("validate", validate);
	});
	useWhenValueChanges(validateOnBlur, function() {
		form.setConfig("validateOnBlur", validateOnBlur);
	});
	var renderProps = {
		form: _extends({}, form, { reset: function reset(eventOrValues) {
			if (isSyntheticEvent(eventOrValues)) form.reset();
			else form.reset(eventOrValues);
		} }),
		handleSubmit: function handleSubmit(event) {
			if (event) {
				if (typeof event.preventDefault === "function") event.preventDefault();
				if (typeof event.stopPropagation === "function") event.stopPropagation();
			}
			return form.submit();
		}
	};
	addLazyFormState(renderProps, state);
	return /* @__PURE__ */ import_react.createElement(ReactFinalFormContext.Provider, { value: form }, renderComponent(_extends({}, rest, { __versions: versions }), renderProps, "ReactFinalForm"));
}
function useForm(componentName) {
	var form = import_react.useContext(ReactFinalFormContext);
	if (!form) throw new Error((componentName || "useForm") + " must be used inside of a <Form> component");
	return form;
}
function useFormState(_temp) {
	var _ref = _temp === void 0 ? {} : _temp, onChange = _ref.onChange, _ref$subscription = _ref.subscription, subscription = _ref$subscription === void 0 ? all$1 : _ref$subscription;
	var form = useForm("useFormState");
	var onChangeRef = import_react.useRef(onChange);
	onChangeRef.current = onChange;
	var _React$useState = import_react.useState(function() {
		return form.getState();
	}), state = _React$useState[0], setState = _React$useState[1];
	import_react.useEffect(function() {
		return form.subscribe(function(newState) {
			setState(function(prevState) {
				if (!shallowEqual(newState, prevState)) {
					if (onChangeRef.current) onChangeRef.current(newState);
					return newState;
				}
				return prevState;
			});
		}, subscription);
	}, []);
	var lazyState = {};
	addLazyFormState(lazyState, state);
	return lazyState;
}
var _excluded$1 = ["onChange", "subscription"];
function FormSpy(_ref) {
	var onChange = _ref.onChange, subscription = _ref.subscription, rest = _objectWithoutPropertiesLoose(_ref, _excluded$1);
	var reactFinalForm = useForm("FormSpy");
	var state = useFormState({
		onChange,
		subscription
	});
	if (onChange) return null;
	return renderComponent(_extends({}, rest, _extends({}, state, { form: _extends({}, reactFinalForm, { reset: function reset(eventOrValues) {
		if (isSyntheticEvent(eventOrValues)) reactFinalForm.reset();
		else reactFinalForm.reset(eventOrValues);
	} }) })), state, "FormSpy");
}
var isReactNative = !!(typeof window !== "undefined" && window.navigator && window.navigator.product && window.navigator.product === "ReactNative");
var getSelectedValues = function getSelectedValues(options) {
	var result = [];
	if (options) for (var index = 0; index < options.length; index++) {
		var option = options[index];
		if (option.selected) result.push(option.value);
	}
	return result;
};
var getValue = function getValue(event, currentValue, valueProp, isReactNative) {
	if (!isReactNative && event.nativeEvent && event.nativeEvent.text !== void 0) return event.nativeEvent.text;
	if (isReactNative && event.nativeEvent) return event.nativeEvent.text;
	var _detypedEvent$target = event.target, type = _detypedEvent$target.type, value = _detypedEvent$target.value, checked = _detypedEvent$target.checked;
	switch (type) {
		case "checkbox": if (valueProp !== void 0) if (checked) return Array.isArray(currentValue) ? currentValue.concat(valueProp) : [valueProp];
		else {
			if (!Array.isArray(currentValue)) return currentValue;
			var index = currentValue.indexOf(valueProp);
			if (index < 0) return currentValue;
			else return currentValue.slice(0, index).concat(currentValue.slice(index + 1));
		}
		else return !!checked;
		case "select-multiple": return getSelectedValues(event.target.options);
		default: return value;
	}
};
function useLatest(value) {
	var ref = import_react.useRef(value);
	import_react.useEffect(function() {
		ref.current = value;
	});
	return ref;
}
/**
* Creates a callback, even with closures, that will be
* instance === for the lifetime of the component, always
* calling the most recent version of the function and its
* closures.
*/
function useConstantCallback(callback) {
	var ref = import_react.useRef(callback);
	import_react.useEffect(function() {
		ref.current = callback;
	});
	return import_react.useCallback(function() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		return ref.current.apply(null, args);
	}, []);
}
var all = fieldSubscriptionItems.reduce(function(result, key) {
	result[key] = true;
	return result;
}, {});
var defaultFormat = function defaultFormat(value, _name) {
	return value === void 0 ? "" : value;
};
var defaultParse = function defaultParse(value, _name) {
	return value === "" ? void 0 : value;
};
var defaultIsEqual = function defaultIsEqual(a, b) {
	return a === b;
};
function useField(name, config) {
	if (config === void 0) config = {};
	var _config = config, afterSubmit = _config.afterSubmit, allowNull = _config.allowNull, component = _config.component, data = _config.data, defaultValue = _config.defaultValue, _config$format = _config.format, format = _config$format === void 0 ? defaultFormat : _config$format, formatOnBlur = _config.formatOnBlur, initialValue = _config.initialValue, multiple = _config.multiple, _config$parse = _config.parse, parse = _config$parse === void 0 ? defaultParse : _config$parse, _config$subscription = _config.subscription, subscription = _config$subscription === void 0 ? all : _config$subscription, type = _config.type, validateFields = _config.validateFields, _value = _config.value;
	var form = useForm("useField");
	var configRef = useLatest(config);
	var register = function register(callback, silent) {
		return form.registerField(name, callback, subscription, {
			afterSubmit,
			beforeSubmit: function beforeSubmit() {
				var _configRef$current = configRef.current, beforeSubmit = _configRef$current.beforeSubmit, formatOnBlur = _configRef$current.formatOnBlur, _configRef$current$fo = _configRef$current.format, format = _configRef$current$fo === void 0 ? defaultFormat : _configRef$current$fo;
				if (formatOnBlur) {
					var fieldState = form.getFieldState(name);
					if (fieldState) {
						var value = fieldState.value;
						var formatted = format(value, name);
						if (formatted !== value) form.change(name, formatted);
					}
				}
				return beforeSubmit && beforeSubmit();
			},
			data,
			defaultValue,
			getValidator: function getValidator() {
				return configRef.current.validate;
			},
			initialValue,
			isEqual: function isEqual(a, b) {
				return (configRef.current.isEqual || defaultIsEqual)(a, b);
			},
			silent,
			validateFields
		});
	};
	var _React$useState = import_react.useState(function() {
		var existingFieldState = form.getFieldState(name);
		if (existingFieldState) {
			if (allowNull && existingFieldState.initial === null && existingFieldState.value !== null) return _extends({}, existingFieldState, {
				value: null,
				initial: null
			});
			return existingFieldState;
		}
		var initialStateValue = initialValue;
		if (component === "select" && multiple && initialValue === void 0) initialStateValue = [];
		return {
			active: false,
			blur: function blur() {},
			change: function change() {},
			data: data || {},
			dirty: false,
			dirtySinceLastSubmit: false,
			error: void 0,
			focus: function focus() {},
			initial: initialStateValue,
			invalid: false,
			length: void 0,
			modified: false,
			modifiedSinceLastSubmit: false,
			name,
			pristine: true,
			submitError: void 0,
			submitFailed: false,
			submitSucceeded: false,
			submitting: false,
			touched: false,
			valid: true,
			validating: false,
			value: initialStateValue,
			visited: false
		};
	}), state = _React$useState[0], setState = _React$useState[1];
	import_react.useEffect(function() {
		return register(function(newState) {
			setState(function(prevState) {
				if (!shallowEqual(newState, prevState)) return newState;
				return prevState;
			});
		}, false);
	}, [
		name,
		data,
		defaultValue,
		initialValue
	]);
	var meta = {};
	addLazyFieldMetaState(meta, state);
	var getInputValue = function getInputValue() {
		var value = state.value;
		if (value === null && !allowNull) value = "";
		if (formatOnBlur) {
			if (component === "input") value = defaultFormat(value);
		} else if (!(allowNull && value === null)) value = format(value, name);
		if (component === "select" && multiple) return Array.isArray(value) ? value : [];
		if ((type === "checkbox" || type === "radio") && _value !== void 0) return _value;
		return value;
	};
	var getInputChecked = function getInputChecked() {
		var value = state.value;
		if (type === "checkbox") {
			value = format(value, name);
			if (_value === void 0) return !!value;
			else return !!(Array.isArray(value) && ~value.indexOf(_value));
		} else if (type === "radio") return format(value, name) === _value;
	};
	var input = {
		name,
		onBlur: useConstantCallback(function(_event) {
			state.blur();
			if (formatOnBlur) {
				/**
				* Here we must fetch the value directly from Final Form because we cannot
				* trust that our `state` closure has the most recent value. This is a problem
				* if-and-only-if the library consumer has called `onChange()` immediately
				* before calling `onBlur()`, but before the field has had a chance to receive
				* the value update from Final Form.
				*/
				var fieldState = form.getFieldState(state.name);
				if (fieldState) state.change(format(fieldState.value, state.name));
			}
		}),
		onChange: useConstantCallback(function(event) {
			// istanbul ignore next
			if (event && event.target) {
				var targetType = event.target.type;
				var unknown = ~[
					"checkbox",
					"radio",
					"select-multiple"
				].indexOf(targetType) && !type && component !== "select";
				var _value2 = targetType === "select-multiple" ? state.value : _value;
				if (unknown) console.error("You must pass `type=\"" + (targetType === "select-multiple" ? "select" : targetType) + "\"` prop to your Field(" + name + ") component.\n" + ("Without it we don't know how to unpack your `value` prop - " + (Array.isArray(_value2) ? "[" + _value2 + "]" : "\"" + _value2 + "\"") + "."));
			}
			var value = event && event.target ? getValue(event, state.value, _value, isReactNative) : event;
			state.change(parse(value, name));
		}),
		onFocus: useConstantCallback(function(_event) {
			return state.focus();
		}),
		get value() {
			return getInputValue();
		},
		get checked() {
			return getInputChecked();
		}
	};
	if (multiple) input.multiple = multiple;
	if (type !== void 0) input.type = type;
	return {
		input,
		meta
	};
}
var _excluded = [
	"afterSubmit",
	"allowNull",
	"beforeSubmit",
	"children",
	"component",
	"data",
	"defaultValue",
	"format",
	"formatOnBlur",
	"initialValue",
	"isEqual",
	"multiple",
	"name",
	"parse",
	"subscription",
	"type",
	"validate",
	"validateFields",
	"value"
];
function FieldComponent(_ref, ref) {
	var afterSubmit = _ref.afterSubmit, allowNull = _ref.allowNull, beforeSubmit = _ref.beforeSubmit, children = _ref.children, component = _ref.component, data = _ref.data, defaultValue = _ref.defaultValue, format = _ref.format, formatOnBlur = _ref.formatOnBlur, initialValue = _ref.initialValue, isEqual = _ref.isEqual, multiple = _ref.multiple, name = _ref.name, parse = _ref.parse, subscription = _ref.subscription, type = _ref.type, validate = _ref.validate, validateFields = _ref.validateFields, value = _ref.value, rest = _objectWithoutPropertiesLoose(_ref, _excluded);
	var field = useField(name, {
		afterSubmit,
		allowNull,
		beforeSubmit,
		component,
		data,
		defaultValue,
		format,
		formatOnBlur,
		initialValue,
		isEqual,
		multiple,
		parse,
		subscription,
		type,
		validate,
		validateFields,
		value
	});
	if (typeof children === "function") return children(_extends({}, field, rest));
	if (typeof component === "string") {
		var inputProps = _extends({}, field.input);
		if (component === "select" && multiple && !Array.isArray(inputProps.value)) inputProps.value = [];
		return /* @__PURE__ */ import_react.createElement(component, _extends({}, inputProps, {
			children,
			ref
		}, rest));
	}
	if (!name) throw new Error("prop name cannot be undefined in <Field> component");
	return renderComponent(_extends({
		children,
		component
	}, rest, field), {}, "Field(" + name + ")");
}
var Field = /* @__PURE__ */ import_react.forwardRef(FieldComponent);
function withTypes() {
	return {
		Form: ReactFinalForm,
		FormSpy
	};
}
//#endregion
export { Field, ReactFinalForm as Form, FormSpy, useField, useForm, useFormState, version, withTypes };

//# sourceMappingURL=react-final-form.js.map